@model List<Category>

    <div class="row">
        @foreach (Category category in Model)
        {
            <div class="col-lg-6">
                <a href="#">@category.Name</a>
            </div>
        }
    </div> 